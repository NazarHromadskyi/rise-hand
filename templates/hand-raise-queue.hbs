<div class="rise-hand-queue-container" data-application-part="form">
  <div class="queue-header">
    <h2>🔊 Hand Raise Queue</h2>
    {{#if isGM}}
      {{#if hasQueue}}
        <button data-action="clearAll" class="clear-all-btn">{{clearAll}}</button>
      {{/if}}
    {{else}}
      {{#if isCurrentUserInQueue}}
        <button data-action="leaveQueue" class="leave-queue-btn">{{leaveQueue}}</button>
      {{/if}}
    {{/if}}
  </div>
  
  <div class="queue-content">
    {{#if hasQueue}}
      <div class="queue-list">
        {{#each queue}}
          <div class="queue-item {{#if isUrgent}}urgent{{/if}}">
            <div class="queue-item-header">
              <span class="position-badge">{{position}}</span>
              <span class="priority-icon">{{priorityIcon}}</span>
              <strong class="user-name">{{userName}}</strong>
              <span class="priority-tag {{#if isUrgent}}urgent{{/if}}">{{priorityText}}</span>
            </div>
            <div class="queue-item-details">
              <span class="timestamp">{{timeString}}</span>
            </div>
            {{#if ../isGM}}
              <div class="queue-item-actions">
                <button data-action="removeUser" data-user-id="{{userId}}">{{../remove}}</button>
              </div>
            {{/if}}
          </div>
        {{/each}}
      </div>
    {{else}}
      <div class="empty-queue">
        <p>{{emptyMessage}}</p>
      </div>
    {{/if}}
  </div>
</div> 