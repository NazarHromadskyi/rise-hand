<div class="rise-hand-button-container">
  {{#if canRaise}}
    <div class="button-section">
      <h3>{{buttonText}}</h3>
      <div class="priority-buttons">
        <button id="raise-hand-normal" class="priority-button normal">
          ✋
          {{priorityNormal}}
        </button>
        <button id="raise-hand-urgent" class="priority-button urgent">
          🚨
          {{priorityUrgent}}
        </button>
      </div>
    </div>
  {{else}}
    <div class="queue-status">
      <h3>{{youAreInQueue}}</h3>
      {{#if position}}
        <p class="position">{{queuePosition}}</p>
      {{/if}}
      <button id="lower-hand" class="leave-button">
        {{leaveQueue}}
      </button>
    </div>
  {{/if}}
</div>