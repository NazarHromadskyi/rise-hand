<div class="rise-hand-button-container" data-application-part="form">
  {{#if canRaise}}
    <div class="button-section">
      <h3>{{buttonText}}</h3>
      <div class="priority-buttons">
        <button data-action="raiseNormal" class="priority-button normal">
          ✋
          {{priorityNormal}}
        </button>
        <button data-action="raiseUrgent" class="priority-button urgent">
          🚨
          {{priorityUrgent}}
        </button>
      </div>
    </div>
  {{else}}
    <div class="queue-status">
      <h3>{{youAreInQueue}}</h3>
      {{#if position}}
        <p class="position">{{queuePosition}}</p>
      {{/if}}
      <button data-action="lowerHand" class="leave-button">
        {{leaveQueue}}
      </button>
    </div>
  {{/if}}
</div>